# Copy this file to ".env.local" and adjust. Never commit ".env.local".
#
# Use images/PDFs from public/ instead of the live site:
# NEXT_PUBLIC_USE_LOCAL_ASSETS=true
#
# Leave unset or false to load assets from the public website (see src/lib/siteAssets.ts).
